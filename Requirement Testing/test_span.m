classdef test_span < sltest.TestCase

   methods (Test)
       function test1(testCase)
%           model = 'vdp';
%           testCase.loadSystem(model);
%           simOut = testCase.simulate(model);
%           testCase.verifySignalsMatch(simOut,'baseline_vdp.mat');
       end
   end
   
end
