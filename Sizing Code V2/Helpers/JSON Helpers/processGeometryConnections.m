function geom = processGeometryConnections(geom)
    % need to look through every part of geom
        % 1. is the current variable derived?
            % if yes -> take its path (string chain) and save it to the deriving variable
                % if the deriving variable is also derived, loop through each of the derived variables listed. It might be assigned to any
                % primary variable
            % if no -> just ignore it

    % TODO: This has not been implemented at all lol

    warning("Connections not implemented yet")

    geom = geom;

end