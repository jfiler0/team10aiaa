function cruise_drag_breakdown(perf, N, W)

% Step 1: Build a full performance map (up to M2)
% Step 2: Use this to compute the optimal altitude for a range up to the max mach number
% Step 3: Get the drag components for this condition vector
% Step 4: Plot as a ribbon plot

[~, M_max] = compute_max_mach(perf, W);






end